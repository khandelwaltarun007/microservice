spring:
  application:
    name: order-service
  cloud:
    discovery:
      enabled: true
    config:
      uri: http://localhost:8888  # Config Server URL
      name: order-service
      profile: default
      label: null