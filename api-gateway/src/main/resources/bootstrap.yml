spring:
  application:
    name: api-gateway
  cloud:
    discovery:
      enabled: true
    config:
      uri: http://localhost:8888  # Config Server URL
      name: api-gateway
      profile: default
      label: null